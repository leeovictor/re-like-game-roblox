--!strict

export type Tokens = {
	background: Color3,
	panel: Color3,
	input: Color3,
	popup: Color3,
	item: Color3,
	outline: Color3,
	outlineTransparency: number,
	text: Color3,
	mutedText: Color3,
	accent: Color3,
	errorText: Color3,
	scrim: Color3,
	scrimTransparency: number,
	buttonHeight: number,
	inputHeight: number,
	cornerRadius: number,
	fieldSpacing: number,
	labelSpacing: number,
	cardPadding: number,
	listMaxHeight: number,
	titleTextSize: number,
	textSize: number,
	smallTextSize: number,
}

local CameraSystemStyle: Tokens = {
	background = Color3.fromRGB(24, 25, 29),
	panel = Color3.fromRGB(45, 48, 55),
	input = Color3.fromRGB(32, 34, 39),
	popup = Color3.fromRGB(22, 24, 28),
	item = Color3.fromRGB(40, 43, 50),
	outline = Color3.fromRGB(74, 80, 92),
	outlineTransparency = 0.25,
	text = Color3.fromRGB(235, 235, 235),
	mutedText = Color3.fromRGB(150, 155, 165),
	accent = Color3.fromRGB(70, 90, 125),
	errorText = Color3.fromRGB(255, 120, 120),
	scrim = Color3.fromRGB(0, 0, 0),
	scrimTransparency = 0.5,
	buttonHeight = 26,
	inputHeight = 26,
	cornerRadius = 4,
	fieldSpacing = 10,
	labelSpacing = 6,
	cardPadding = 10,
	listMaxHeight = 140,
	titleTextSize = 15,
	textSize = 13,
	smallTextSize = 12,
}

return CameraSystemStyle
